# Current session handoff

## What happened

**Codex round-6 docs review fully resolved.** All 5 findings in `codex-review.md` adversarially validated (5 parallel agents): 4 confirmed, 1 partially confirmed — P1-2's delisted-series example was refuted (the conservative `terminal-unscorable` rule already closes factless disappearances); the real gap was the never-covered symbol + the missing transient-vs-disappearance discriminator. All 5 forks user-decided via selector, recommended option on all: **entry-stamped sector identity** (sector label + resolved SPDR benchmark symbol on every durable episode/matrix/archive record, typed `sector-unscorable`, no label-time classification call); **price-coverage grace** (drafted ~3 months past window end, homed in Portfolio §Starting parameters) closing a pending price leg as typed `price-coverage-unscorable` and doubling as the disappearance discriminator; **save gate scoped** to the cloud agent/token submission with the provider-credential save split named a code prerequisite of the queued Local-analysis-models Settings slice (Settings.vue/settings.rs comments note the docs-leads-code gap); **`no-dominant-mode` guarded on input completeness** (`resolution-unscorable` generalized — live metric-unscorable the live form, post-departure the limiting case; precedence unchanged for evaluable branches); **pass-agnostic `departed`** (DTO Step 7 or ATO Deep Audit — ATO archival atomically transitions the touched node, graph negation scoped to discovery-wide rebuilds). Fixes in `38f6bd7` (9 docs + 2 code comments + fixes-file round-6 table + 9 INDEX rows). Verified: 1,196 relative links / 0 broken, `npm run build` green, `npm test` 152/152, `cargo test` + clippy green. Also: **CODEX-DOCS-REVIEW.md Pass 3 retired** (`960a109`) — report group review-complete per run 6's clean result, next review is two-pass, run counter → 7; **BUILD.md §What remains** gained the save-split prerequisite on the Settings slice (`567d243`). All pushed.

## Current state

`main` @ `567d243` plus this handoff commit, pushed; tree clean apart from the untracked local `codex-review.md`. Nothing in flight. Queue head unchanged: **the fund-slice plan** (`/metis-plan-task` against `docs/portfolio-analysis.md §Asset eligibility`), accounting for the two named code prerequisites — the ticker→CIK resolver and the holdings book-level netting step.

## Open questions

- **Fund-form scenario-target methodology (blocking)** — the fund-slice plan's first decision.
- **Local-suite scorecard display (carried, deferred)** — TO shadow + Portfolio outcome scorecard UI surfaces.
- **Encrypted-archive live round-trip (carried, optional)** — one passphrase export→import before the M5 move.
- **Dev-app sanity residue (carried)** — table-head glyph hit-target click (needs portfolio data).
- **Keychain fail-soft candidate (carried)** — denied Keychain read blanks local warning categories for the session.
- **Stage-and-swap import hardening (carried)** — named, not scheduled.
- **First post-v0.31.2 Ollama release (carried)** — #14645 fix + `think:true`+`tools` coverage before pinning the M5 version.
- **Chain both-maps invariant (carried)** — tighten once a live `/chains` response confirms both maps.
- **Long/cold-start 600s stress (carried)** — only a long/cold-start report could near 600s.
- **Local-model M5 pre-flight + M5-calibration (carried)** — 122B load/backend, `num_ctx`, throughput, long-context probe; Stooq refresh + benchmark/futures symbol/adjustment live-verify incl. gold `gc.f` / silver `si.f`; `continuity_weight` bands, thresholds, budgets; the release→event map's FMP strings join the paid-key checkpoint.
- **Four-part verdict + bidirectional-conviction bound; §1 open drafts; M5-gated backlog (carried)** — land with full Portfolio + TO.
- **Legacy-docs broken anchor (carried, trivial)** — `legacy_docs/NOTES-FROM-RESTRUCTURING.md:15` → `docs/scheduling.md#job-controls` (heading gone); repair or retire whenever convenient.

## Where to start

Run **`/metis-plan-task` for the fund slice** against `docs/portfolio-analysis.md §Asset eligibility`; settle the **fund-form scenario-target methodology** first. Treat the round-2 through round-6 typed contracts as spec, not open design (round 6 added: the entry-stamped sector identity + `sector-unscorable`, the price-coverage grace + `price-coverage-unscorable`, the residual's input-completeness guard, pass-agnostic `departed`, the scoped save gate), and account for the two named code prerequisites (CIK resolver, holdings netting). If another Codex round runs first, it is now **two-pass** (Pass 3 retired) and triages through `claude-code-fixes.md` — match by content, not finding ID (IDs restart every round; round 6's table is now the top section).
