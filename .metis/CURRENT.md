# Current session handoff

## What happened

**B10+B13, the card-display pair, is COMPLETE as one combined UI slice** — planned with nine plan-time user rulings, built, internal review **approve** (one round: the 760px monitor-stacking rules were cascade-dead behind later base rules — moved to a post-base media block), two Codex rounds (round 1: sr-only exposure added; round 2 ruled hover+sr-only still insufficient for sighted keyboard / low-vision users → the **always-visible caption** "Setup — market-setup read, outside the letter" replaced both tooltip and sr-only), committed `1bc21d2`, BUILD/INDEX aligned `240ce15`, pushed.
The rulings that shaped it: one combined slice; the momentum tile stays in-row set apart behind a hairline as **"Setup"**; the monitor renders on **both** card branches (role-risk condition-only — target line drops on the structural null) plus the monitor-level goalposts dl; historical views render it (run content, not live state); partial monitors render as-authored; kit deviations recorded convention-only (deviations memory #10 — kit `SubScores`/`Scenarios` untouched, Codex will re-flag, known-accepted); no new big-run watch — the monitor row + setup tile ride the existing card-render-at-scale watch.
Verified: cargo 922 lib + 32 integration / 0 fail, clippy 0, npm build, 40 node + 213 vitest.

## Current state

**The 2026-08-04 B-ruling build queue is empty** (B12, B3, B7, B10+B13 all landed). Queue: **review piece 3 (own session) → the big confirmation run.**

## Open questions

- **Big-run watches from B3** — (1) slash-notation class shares (`BRK/B`) read Unresolved → not-rated under the verbatim FMP lookup; (2) ticker-noise descriptions ("NTDOF COM") risk a false Conflict. Real Schwab shapes on the big run settle whether either needs a rider.
- **Big-run watches (construction leg)** — unchanged: lean-divergence / engine-bar / carried-stale-lean rates, construction-prompt fit (instrumented), overlay classification vs real OCC rows, 7b decided-range movement rate — the run banks B7's changed profile framing, and the card-render watch now also covers the monitor strip + Setup caption at 47-position scale.
- **Research-loop activation obligation** — identity + source-text validation + period normalization before the pre-profit producer activates.
- **Standing** — unchanged carried list: live-run calibration watches (STI-reads-zero, YoY contiguity, outcome-leg watches), no A letters under grade-v2, big-run checklist, reasoning-pane DOM weight, encrypted portability round-trip, step-17 embedding, 600 s stress, scorecard display, dev-store residue, Keychain fail-soft, stage-and-swap import, chain both-maps invariant, four-part verdict bound, §1 open drafts, fraud-producer posture + the `hard_forensic_bar` consumer seam, fund-slice drafted constants, checkpoint/resume + the 6g input-delta validator.

## Where to start

**Review piece 3 — the value-chain correctness walk — in its own session** (the last pre-run item; a review, not a build). Then the single big confirmation run per the locked plan, in the dev app (process name `market-signal`), reading the data-health deep-history line for the Stooq-PoW / rung-order question.
