# Current session handoff

## What happened

**The research-loop slice shipped** — planned/implemented last session, this
session it went through **eight Codex review rounds to approval** and merged
as PR #70 (squash `457efe1`); Portfolio Analysis is now **built in full** and
the pre-run bar is met. The rounds hardened every deterministically checkable
leg (SSRF special-use ranges, cache-reads re-passing URL policy, a
consent-honest SearXNG probe, page-grounded typed channels with
stated-endpoint corroboration and monetary unit typing, a structural identity
matcher — ticker context + proper-noun runs — seed-layer reconciliation with
stale-row deletion, attempt-counted budgets, an epoch-guarded launch
preflight). The three semantic P1 classes closed by the **2026-08-24 channel
rulings** (decided with Codex): forward assumption **shadow-only** suite-wide
(would-have audit lines; `engine_output` immutable past 6b), research-fed
fraud **advisory** (hard-forensic state reads the filing kinds alone), and
the indicator's hype-cap anchor gated on an app-verified ledger `driver_id`.
Record: `docs/verification/2026-08-24-research-loop-rulings.md`. BUILD and
INDEX were updated (user-authorized): research loop → §Built, queue
renumbered (big run 1, TO 2), rulings folded into §Standing constraints.
Gates at merge: cargo 1185/0 (+demo-run), clippy clean, npm build clean,
npm test 46/46 + 247/247.

## Current state

Working tree clean on `main` at `457efe1`; nothing in flight. `PROMPT_VERSION`
stays `portfolio-v12` (no record was ever persisted under it, so the
review-round schema/prompt changes ride the same stamp). The queue: **the
single big confirmation run** (attempt 3), then Trade Opportunities.

## Open questions

- **Big-run watch set** (`docs/verification/big-run-watch-set.md`) still needs
  its standing additions before the run: research-loop, pre-profit-activation,
  CBOE-backdrop, and narrative-comparator lines; the prompt-stamp note
  v11 → v12; the Step-6a-empty note; the Schwab-CEF-typing watch; and the
  three 2026-08-24 ruling watches (shadow-assumption resolutions,
  unverified-driver indicator gaps, advisory fraud claims).
- The 6e **supersede leg is structurally dead as-built** (the consensus feed
  carries no as-of date, so it always rejects on that named condition) — now
  doc-caveated at §Step 6e, but still an §Awaiting-a-ruling candidate if a
  dated consensus source ever lands.
- **Channel promotion criteria** are deliberately open: the shadow assumption
  promotes only after manually inspected shadow cases; advisory fraud promotes
  on explicit acknowledgment or a structured defendant-field adapter; the
  first-party host check's alias gap waits on an issuer-website profile field.
- Research budgets (40 attempts / 30 min per holding, 4k-char seed budget)
  calibrate on first live runs.

## Where to start

Fold the queued watch-set additions into
`docs/verification/big-run-watch-set.md` (the list above; read `data-health`
early per the standing note), then launch **big confirmation run attempt 3**
per that checklist and write its dated record under `docs/verification/`.
