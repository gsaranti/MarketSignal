# Current session handoff

## What happened

**The 14 B rulings are all dispositioned** (per-item walkthrough, user-ruled; recorded in the piece-2 record's new §Rulings).
**9 doc outcomes applied** across five docs (`a5992f4`): trailing-TTM payout basis, both evidence-floor arms designed, 6f existence-flag render (prior-value rendering = named rider on the future 6g input-delta validator slice), vector-continuity lane designed at both ends, whole-chain options method (25-delta spec = calibration-owned target), manual import designed, CEF leg designed w/ `nav_premium` unconsumed, strata recorded-not-derived, unconditional fund quick-refresh retrieval.
**5 build outcomes**: **B12 built in-session** (same commit + one Codex round) — `prompt_eval_count` captured on both chat paths, per-stage `PromptUsage` w/ sent `prompt_chars` behind a defaulted `HoldingAnalyst::take_prompt_usage`, `DataHealth.context_pressure` + always-recorded `peak_prompt`, **two triggers**: near-full ≥90%-of-`num_ctx` AND the Codex-confirmed chars-vs-count front-truncation check (post-truncation counts land far *below* `num_ctx` — the preflight 1,026-of-2,048 signature fill alone cannot see; ×8 implausibility bound).
BUILD + INDEX aligned in-session at user request (`8626bae`).
Verified: cargo 941 / 0 fail, clippy 0, npm build, 40 node + 206 vitest. Both commits pushed.

## Current state

Queue (user decision: rest of the B builds in a new session): **B3 → B7 → B10+B13, then review piece 3, then the big confirmation run.**

- **B3 listing-resolution guard** — the substantial slice; loop-time FMP issuer cross-check (the outcome slice's fail-soft `/profile` plumbing exists), two documented outcomes (no-resolution / non-US → not-rated `unsupported-listing`; resolved-but-conflicting → insufficient-evidence). **Note: the B2 doc edit already says the identity arm's producer is "the listing-resolution guard slice, in build" — B3 must land or that line goes stale.**
- **B7 profile alignment** — `objective` field + medium-to-high default + 7b render + read-only Settings block; a deliberate change to the framing the big run banks.
- **B10 + B13** — card-display pair (momentum tile set apart as market-setup; MonitorScenario render), plausibly one UI slice under the design package + frontend-craft.

The big run now additionally live-exercises B12's instrumentation (peak fill + pressure/truncation flags on data-health answer the construction-fit watch directly).

## Open questions

- **B7 scope confirm** — the ruled option covered field + mapping + 7b render; the read-only Settings block was assumed in from the record's original option — confirm at plan time.
- **B10+B13 packaging** — one combined UI slice or two; decide at plan time.
- **New big-run watches (construction leg)** — unchanged: lean-divergence / engine-bar / carried-stale-lean rates, construction-prompt fit (now measured), overlay classification vs real OCC rows, 7b decided-range movement rate.
- **Research-loop activation obligation** — identity + source-text validation + period normalization before the pre-profit producer activates.
- **Standing** — unchanged carried list: live-run calibration watches (STI-reads-zero, YoY contiguity, outcome-leg watches), no A letters under grade-v2, big-run checklist, reasoning-pane DOM weight, encrypted portability round-trip, step-17 embedding, 600 s stress, scorecard display, dev-store residue, Keychain fail-soft, stage-and-swap import, chain both-maps invariant, four-part verdict bound, §1 open drafts, fraud-producer posture + the `hard_forensic_bar` consumer seam, fund-slice drafted constants, checkpoint/resume + the 6g input-delta validator.

## Where to start

**`/metis-plan-task` on B3** (the listing-resolution guard): plan the loop-time issuer cross-check on the existing `/profile` plumbing, the name-comparison rule (exact match too strict — "Apple Inc" vs "APPLE INC COM"), the two documented routing outcomes, and tests; it also lands the producer the B2 doc edit references. Then B7, then the B10+B13 pair, then piece 3 in its own session, then the big run per the locked plan.
