# Current session handoff

## What happened

A short between-slices session. The **big-run watch set is now complete**:
every queued addition was folded into
`docs/verification/big-run-watch-set.md` (user-authorized) — a new §The
research loop (first-live-loop watches, the three budget constants
verified against code, typed-channel validation yield, seed-and-merge
caching, and the three 2026-08-24 ruling watches), the
pre-profit-activation line, the narrative-comparator debut line, the
CBOE-backdrop probe, the Schwab-CEF-typing watch, the Step-6a-empty note,
and the header's prompt-stamp note (this run's records stamp
`portfolio-v12`). Two calls were made: **rendered retrieval + Connected
Sources stay deferred** behind the run — the watch set gained an
extraction-telemetry line (per-domain `thin_stub` / `extraction_quality`
rates) so the run record's disposition reads the rendered-retrieval
scheduling evidence deliberately, not incidentally; and SearXNG
provisioning was confirmed one-command — the repo ships the pinned compose
bundle at `searxng/`, OrbStack suffices, nothing else to install.

## Current state

The working tree carries one uncommitted edit:
`docs/verification/big-run-watch-set.md` (the additions above). Nothing
else is in flight; `main` at `457efe1`, `PROMPT_VERSION` =
`portfolio-v12`. The queue is unchanged: **big confirmation run attempt
3**, then Trade Opportunities.

## Open questions

- **Runtime auto-start / spin-down** (Ollama + OrbStack/SearXNG around the
  local jobs) — proposed this session, undecided. Assessment given: the
  start leg fits the run-gate posture (probe fails → supervised start →
  bounded health wait, gate semantics unchanged); spin-down needs
  app-ownership flags and cuts against the model-residency default (prefer
  a `keep_alive: 0` unload over killing the daemon); recommended as a
  post-run extension of the guided-setup follow-up. The user has not
  chosen between recording it in BUILD now vs planning it after the run.
- The 6e **supersede leg stays structurally dead as-built** (the consensus
  feed carries no as-of date) — an §Awaiting-a-ruling candidate if a dated
  consensus source ever lands.
- **Channel promotion criteria** deliberately open: shadow assumption
  promotes only after manually inspected shadow cases; advisory fraud on
  explicit acknowledgment or a structured defendant-field adapter; the
  first-party host check's alias gap waits on an issuer-website profile
  field.
- Research budgets (40 attempts / 30 min per holding, 4k-char seed)
  calibrate on the run — now formally lines in the watch set.

## Where to start

Commit the watch-set edit, then launch **big confirmation run attempt 3**:
bring up SearXNG (`cd searxng && docker compose up -d` with OrbStack
running) and Ollama, run per `docs/verification/big-run-watch-set.md`,
read `data-health` early per its §How to read the run, and write the run's
dated record under `docs/verification/`.
