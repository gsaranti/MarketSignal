# Current session handoff

## What happened

**The pre-profit overlay slice SHIPPED and merged (PR #58, squash `f8157f0`).**
Planned via `/metis-plan-task` with a six-question assumptions round — every recommended option taken: observation machinery built **producer-dormant** (research stubbed, candidate list empty); FMP split-consistency verified live on NVDA before coding dilution; missing eligibility inputs = not-entered with the gap recorded; the as-built mappings (post-hoc min-clamp conviction ceiling; severe's lean restriction on the action enum, action = lean until 7b); FCF direct-with-derive-fallback; backend-only (no card, no quick-check leg).
Landed: new `portfolio/pre_profit.rs` (~1.1k lines), the quarterly cash-flow + balance-sheet-cash fetch surface, triple-enforced consequences, `HoldingAudit.pre_profit` persistence w/ selective-carry + abstention retention, prompt contract **portfolio-v5**.
Converged through internal approve-with-nits + Codex rounds 1–3 changes-requested — all seven findings verified-then-fixed, notably the **(period_end, filing_date)-desc statement canonicalization applied to BOTH the overlay and the shared TTM basis** (a served-twice restatement resolves to the latest filing, never wire order), boundary-epsilon thresholds, and two null-fallthrough shaping fixes — then round 4 **approved**.
Verified: cargo 815/0 + integration suites, clippy 0, npm build clean, 40 node + 188 vitest.
**BUILD/INDEX caught up in-session — no capture debt.**

## Current state

`main` at `f8157f0` (+ this session-end), pushed, tree clean, nothing in flight.
Queue head: the **outcome learning** slice.

## Open questions

- **Research-loop activation obligation** (recorded in `pre_profit.rs`'s validator doc comment + BUILD): holding-identity + source-text observation validation and a period-normalization hard rule are mandatory before the pre-profit producer goes live.
- **`driver_ladder` raw quarterly-income order** (engine.rs ~1522 — growth-clamp trailing prints + share basis): the one remaining instance of the wire-order class this slice canonicalized; named unscheduled parity cleanup (Codex round-4 note).
- **Live-run calibration watches**: STI-absent-reads-zero liquid-resources convention; YoY diluted-share quarter-contiguity (index 0 vs 4, no date guard) — both ride the big-run checklist's overlay leg.
- **Docs-capture candidates** (next docs pass): the overlay-survives-abstention retention rule; the every-priced-stock eligibility-record persistence shape.
- **Debut gaps (self-resolve at the big run's persist):** first sweep reads the rate-anchor family `unknown` and pre-basis funds read FundInfo `unknown`.
- **No A letters under grade-v2** (META 84.0 vs ≥ 85) — reserved for normalization or the big run.
- **Carried unchanged:** big-run checklist in BUILD §What remains (now incl. the first live overlay read at 47-position scale); reasoning-pane DOM weight; encrypted portability round-trip; step-17 embedding watch; 600 s stress; scorecard display; dev-store residue; Keychain fail-soft; stage-and-swap import; chain both-maps invariant; four-part verdict bound; §1 open drafts; fraud-producer posture; fund-slice drafted constants; checkpoint/resume + the 6g input-delta validator (docs-promised, unbuilt).

## Where to start

Run `/metis-plan-task` for the **outcome learning** slice (docs/portfolio-analysis.md §Outcome learning + the outcome-learning constants in §Starting parameters; portfolio-workflow.md §Step 7a, §Step 8; storage.md §Local Analysis Suite Storage): recommendation-state-keyed decision episodes with the calibration-feature snapshot, engine-computed labels (total-return primary / price-only common basis, each read on a declared basis and cohort layer), intrinsic calibration keyed on the standalone lean — never the construction-shaped final action — feeding a propose-only calibration.
The two display-only UI micro-slices (Portfolio-page polish; section-scoped footer + report-nav) remain available as breathers.
