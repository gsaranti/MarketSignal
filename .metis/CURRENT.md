# Current session handoff

## What happened

**The v39 fixed-set read ran, the prompt principle was ruled, and the interpretation prompt was rewritten as `portfolio-v40`** (2026-09-17).
The read (64 of 72 calls, user-ended; both engine-set forms) found every explanatory sentence the v35–v39 slices had added being computed with: the margin caps were sized toward and the buffered-threshold habit returned (eight `price-level-mismatch` against one), the hurdle prefix's rate and test were re-run by the model on the indeterminate holdings, and the 3.9 Facts form steered harder on both admission outcomes (not adopted; the List form stays production).
One wrong core was admitted through a seam gap older than the slice: "over" in a window phrase masks the direction (fix list 1.10), and "over N units" is not read as a duration (1.11).
The user then read the rendered prompts and ruled the principle: a prompt never explains the app or its concepts — data, data glosses where a value needs one, the task, the return structure.
The interpretation prompt was drafted to it in three aligned rounds and landed: one message in two marked parts (inputs, then the task in output order), the system prompt the role line plus the output names, no arm / baseline / stage / seam / validator / stamp / product name, caps unshown and the margin sized by three examples, the capital-efficiency read left to the action call, target methods from the typed inputs, a placeholder-only return shape.
Task review rejected twice-over (method strings, an unsurfaced deletion of the stamp history) and both were fixed on rulings; Codex ran three rounds (delta labels, fallback percentiles, the quote's borrowed date, then a false "newer" claim); all fixed.
Commits: `f2f61bc` (the slice), `db646e0` (the shape's named null, key order, array note).
Records: `docs/verification/2026-09-17-interpretation-prompt-rewrite.md`; the §Live read of `2026-09-17-residue-slice.md`; fix list 3.17, 1.10, 1.11 and live lines on 1.9, 3.5–3.14, 8.5.

## Current state

Committed and pushed on `main`, tree clean; Ollama still up from the read.
Debut stamps: `portfolio-v40` / `checkpoint-v10` / `evidence-floor-v5` / `grade-v2.3` / `targets-v6` / `pre-profit-v4` / `quick-check-v4`; portability format 7.
The dev store still holds attempt 6; attempt 7 re-wipes first and must also clear `portfolio_runs` and the `portfolio` namespace of `vector_memory`.
The v40 fixed-set live read is NOT run: three repeats, List form, thinking captured, read against the v38 and v39 tables per the record's §Verification (interpretation markers below 10.2, the level-class downgrades below eight / five / three, every kept core matching, ratios not drifting toward the unshown caps).
The harness reads its daemon roster from the environment: `MARKET_SIGNAL_LOCAL_DAEMON_ENDPOINT=http://localhost:11434 MARKET_SIGNAL_LOCAL_REASONER_MODEL=qwen3.5:122b-a10b MARKET_SIGNAL_LOCAL_EMBEDDER_MODEL=qwen3-embedding:4b` beside the eval variables; launch it detached (nohup) and log under `~/Downloads/market-signal-fixed-evidence-<date>/`.
Rendered prompts for a read before the run: `MARKET_SIGNAL_LOCAL_EVAL_PROMPT_DUMP=<file> cargo test --lib fixed_evidence_prompt_dump -- --ignored` (current dump `~/Downloads/market-signal-fixed-evidence-2026-09-16/prompts-v40.md`).
Queued, each its own plan, in this order unless ruled otherwise: the action prompt (the 3.10 / 3.11 / engine-set findings live there), the role/risk prompt (smallest; shares the rewritten ledger block), the research gathering and synthesis prompts, the distillation prompts; the seam-gap slice (1.10–1.11); then 3.15–3.16, 8.6 after 7.2, §4–§7.
Owed, user-run: BUILD §Built bullets and INDEX rows for the 2026-09-15 (two), 2026-09-16 (three) and 2026-09-17 (two) records, and the §What remains gate sentence.

## Open questions

- Whether the v40 read's yield on PGNY-type holdings falls without the shown caps (the read watches `margin-implausible`); a better example, not the caps, is the answer if it does.
- The 3.9 ENGINE SET line itself: the Facts form is out, but the entry's ruling on the list's wording is open for the action-prompt rewrite.
- Whether the distillation prompts count as one more slice under the principle (they are user-message-only on the fast tier).
- Carried: the prose scan's issuer-losses false positive stays a diagnostic; the fixture README's gain / loss parenthetical (F8); concentration trimming is the planner's; the attempt-6 record erratum; ARKF prices; stop rule 1 unread; per-domain denied share and the SearXNG engine set at book scale; the summary text's ".." artifact; the fixtures carry no fund context, so FUND never renders on the fixed set.

## Where to start

The v40 fixed-set live read, only on the user's word: bring Ollama up per the runbook (it may still be up), set the three daemon variables and the eval variables, run `cargo test fixed_evidence_live -- --ignored --nocapture` detached, read it against the record's §Verification list, and record the findings in `2026-09-17-interpretation-prompt-rewrite.md`.
Never start the read unasked, never restart it unasked.
Do not launch or propose a book-scale attempt, and do not start the next prompt slice unasked.
