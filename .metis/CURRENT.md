# Current session handoff

## What happened

**The Codex full-corpus round is DONE — all 29 findings + the breadth clarification processed** (P1–P12 last session; T1–T10 + R1–R7 this one), each verified against the docs and every fix user-approved. All confirmed. Committed in batches and pushed: Pass-1 retro-batch `0564f4b`, T1–T4 `b5dd7c9`, T5–T7 `83f6c65`, T8–T10 `16cfed5`, R1–R7 `da7819c`, INDEX/BUILD catch-up `f0e988d`. The design-bearing calls: **evidence-floor debut semantics** — a carried live name's inconclusive re-read holds its last verdict, no shadow episode, no freshness stamp (T2); **matrix final assembly moved to Step 7, over the union** — refreshed-tier re-placement, frozen-conviction insertion (T1); **rotation slice non-disableable** + maintenance precedence over discovery diversity (T3); **research-class re-checks removed** (non-recheckable context; canonical `structured`/`filing`/`research` enum) (T4); **Quick Audit never embeds** — Step 9's legs split (T5); a **Step-3b planning-call contract**, planner kept model-led (T6); the TO **request graph closed** — calendar sourced via FRED `/release/dates` Step-2-owned, commodities single-owned, orphan `shares-float-all` dropped (T7); **segment quarterly series filing-derived** via the research lane (T8); **picked decision episodes** — durable lifecycle ids, retention-independent, lifecycle-scoped Step-5b recall (T9); **terminal outcomes from recorded facts only**, else `terminal-unscorable` (T10); the report **enrichment contracts made codeable** — polarity-mapped release table with neutral relation tags, deterministic froth windows/paging/completion-states, single-homed derivation rules, complete payload with absent-never-zero semantics (R1–R4); **Step 3 reduced to the canonical 13 group keys**, news single-owned by Step 7 (R5–R6); the **embedding validator canonical at report Step 4** (R7 = P12's twin); breadth bullets research-conditional. Anchor sweep clean after every batch (~1030 links, 0 broken).

## Current state

`main` @ `f0e988d`, tree clean, pushed. Docs review + independent Codex round both fully closed; INDEX.md (4 new rows + 12 amendments) and BUILD.md (4 clause amendments) caught up. Nothing in flight — the fund-slice plan is unblocked and next in queue.

## Open questions

- **Fund-form scenario-target methodology (blocking)** — what a *priced* fund's scenario targets derive from; the fund-slice plan's first decision.
- **Local-suite scorecard display (carried, deferred)** — whether the TO shadow scorecard and Portfolio outcome scorecard get UI surfaces.
- **Encrypted-archive live round-trip (carried, optional)** — one passphrase export→import before the M5 move.
- **Dev-app sanity residue (carried)** — table-head glyph hit-target click (needs portfolio data).
- **Keychain fail-soft candidate (carried)** — a denied Keychain read blanks the local warning categories for the session.
- **Stage-and-swap import hardening (carried)** — mid-import I/O failure can leave partial files; named, not scheduled.
- **First post-v0.31.2 Ollama release (carried)** — #14645 fix + `think:true`+`tools` coverage; check before pinning the M5 version.
- **Chain both-maps invariant (carried)** — tighten once a live `/chains` response confirms both maps.
- **Long/cold-start 600s stress (carried)** — only a long/cold-start report could near 600s.
- **Local-model M5 pre-flight + M5-calibration (carried)** — 122B load/backend, `num_ctx`, throughput, long-context probe; Stooq refresh, `continuity_weight` bands, thresholds, budgets.
- **Four-part verdict + bidirectional-conviction bound; §1 open drafts; M5-gated backlog (carried)** — land with full Portfolio + TO.

## Where to start

Run **`/metis-plan-task` against `docs/portfolio-analysis.md §Asset eligibility` (fund path)** — the queue's next slice. The first decision inside the plan is the **fund-form scenario-target methodology** (the blocking open question above); everything else in the fund path is audited to convergence and spec-stable after the Codex round.
