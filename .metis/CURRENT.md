# Current session handoff

## What happened

**Planned, dual-reviewed, and shipped the rate-series point-delta + industry-P/E ceiling slice — two independent carried items plus two external-review fixes.** **Item 1 (rate-series change as a point delta):** FRED's per-series `change_pct` was percent-of-prior for *every* series, which misreads any series whose level is already a rate — most starkly GDPNow's SAAR nowcast (`change_pct` ran ~128%). Added a data-keyed **`ChangeMode { Percent, Annualized, PointDelta }`** + `change_mode_for`, parallel to `ANNUALIZED_SERIES`. A new **`RATE_DELTA_SERIES`** set (the 15 percent-unit rates — yields, OAS/curve spreads, fed-funds bounds, breakevens, `EXPINF1YR`, mortgage, `GDPNOW` — **plus the 3 zero-centered FCIs `NFCI`/`ANFCI`/`STLFSI4`** where percent-of-prior is unstable across zero) now reports the point delta `latest − prior`, with the display **name tagged `(Δ pp)` / `(Δ level)`** so the agent reads the unit. The two change treatments are disjoint (pinned by test); `price` untouched. **Item 2:** **`INDUSTRY_PE_MAX` raised 100.0 → 120.0** — inside the probe's clean 106→128 gap, so the borderline 100–106 cyclical-trough aggregates (energy E&P, casinos) are now **kept** while the ≥128 denominator artifacts stay dropped. **Two review-driven fixes:** Codex (external) caught a **Medium both the author and the `metis-task-reviewer` missed** — `pre_research_query` (`vector_memory.rs`) hard-coded `%` on `Quote.change_pct`, emitting contradictory text like `…(Δ pp): 4.3 percent (+0.10%)` for point-delta internals (my "blast radius = baseline JSON only" claim was wrong — a second consumer). Fixed by branching on `is_rate_delta(symbol)` (now `pub(crate)`). Then folded in the related leak: `baseline_delta` now **strips the marker** (via `fred::strip_point_delta_marker`) so the self-describing delta view (`abs_change`/`pct_change`) doesn't carry a `(Δ pp)` tag that contradicts its percent figure. Squash-merged to **`main` @ `551e26b`**, pushed, branch deleted.

## Current state

**Shipped and pushed.** `main` = `551e26b`, working tree clean, in sync with `origin/main`. Nothing in flight. Verified green: `cargo test` **361 lib** (+8 new this slice) + all integration suites, `cargo clippy --all-targets --all-features` clean. **Not run:** the `#[ignore]`d live `fred_baseline_smoke` (FMP/FRED 250/day quota discipline) — offline tests are the gate.

## Open questions

- *(new, medium-ish — Codex's open question)* `change_pct` remains an **overloaded field**: it carries a percent for most series but a point delta for rate series, so every non-JSON formatter must know the convention (the `pre_research_query` fix reaches into `fred::is_rate_delta` to cope). The structural resolution is a **typed change field on `Quote`** (`change` + a `ChangeKind`/unit), which would erase the name-marker hack entirely — baseline JSON, `pre_research_query`, *and* the delta view all self-describing, no `strip_point_delta_marker` needed. Its own cross-cutting slice (every `Quote` constructor across fred/fmp/bls + serde + tests); deliberately deferred. The marker + `is_rate_delta` coupling is the honest interim.
- *(carried, low)* The `expected` calendar field is a perpetually-`None` slot, research-sourced **by deliberate decision** — no structured code path; all sources (paid API, LLM extraction, structured inbox) declined.
- *(carried, low / parked)* Truncation **#2** (independent self-cap outliving the 30-report cascade) dropped — telemetry stays cascade-only. `cargo fmt` dirty repo-wide; esbuild/vite advisory.

## Where to start

Nothing owed — `main` is clean and pushed. **`BUILD.md` is now stale w.r.t. this slice** and is the most useful next action: its adapters section still says `change_pct` keeps "the same percent-of-prior convention as every other series" / GDPNow's change reads ~128% / `INDUSTRY_PE_MAX` 100.0. Reconcile it to the shipped state (the `ChangeMode`/`RATE_DELTA_SERIES` point-delta treatment + `(Δ pp)`/`(Δ level)` markers + `strip_point_delta_marker`, `INDUSTRY_PE_MAX` 120, and the `vector_memory`/`baseline_delta` consumer fixes). Then open a fresh direction, or take the **typed-change-field** slice (resolves the overloaded-`change_pct` open question), or the low-priority `expected`-field leftover.
