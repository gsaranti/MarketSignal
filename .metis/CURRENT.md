# Current session handoff

## What happened

Built the **analyst layer (Steps 12–16)** — the documented multi-agent core that was entirely absent (the pipeline ran single-agent; `generate_report` called the main agent once with nothing to synthesize over). Scoped to **analyst stage only** (the 16-skill progressive-disclosure library deferred). Shipped to `main` @ `9d5f633` (branch → squash-merge → push → branch deleted). Three analyst agents (Bull/Bear/Balanced) run **concurrently via scoped OS threads — off `tokio`**, honoring the spine's blocking-call discipline — over the **same** Step-11 condensed packet; their reviews ride into the main agent on `MainAgentInput.analyst_reviews` → Step-16 synthesis, and are **ephemeral** (never persisted, §Step 12). **Deliberately NOT fail-soft** (doc-grounded §Step 9): a failing analyst — and, after the Codex fix below, a blank-summary review — fails the run, unlike the fail-soft research half. Key discovery: config keys (`bull/bear/balanced_agent_model`), the four-agent validation gate, and the Settings dropdowns **already modeled four agents**, so **no gate/Settings/frontend work was needed** — this was purely the agent stage + adapter + wiring. metis-task-reviewer **approved**; external Codex review (iris-codex-last.md) caught one real gap — lenient parsing let an empty review pass as success — which I verified, agreed on principle, and fixed (scoped tighter than Codex: validate non-blank summary, keep lists lenient).

## Current state

**Shipped and pushed.** `main` = `9d5f633`, working tree clean, nothing in flight. New `analyst_agent.rs` (`ModelAnalystAgent`, dual-provider, non-streaming, reuses `model_agent`'s request/transport/extractors); `AnalystAgent` trait + `AnalystOutput`/`Posture`/`Confidence` + `StubAnalystAgent` (agent.rs); `AnalystStages` bundle (stub/live) + `run_analysts` (pipeline.rs); `analyst_config(posture)` + three configs on `RunConfig` (boxed `ScheduledRun::Proceed` for `large_enum_variant`) wired in both manual + scheduled `lib.rs` paths; `run_job` threads it. Codex fix: dropped `ReviewEnvelope` field defaults + `envelope_to_output` rejects a blank summary (lists stay lenient). Verified green: `cargo test` **377 lib** (363→377) + all integration suites (incl. `analyst_reviews_reach_the_agent_input`, `a_failing_analyst_fails_the_run`); `cargo clippy --all-targets --all-features` clean. Frontend untouched (no `npm` needed). `#[ignore]`d live smokes not run.

## Open questions

- *(resolved this session)* **BUILD.md now folds in the analyst layer** — a targeted update (load-bearing as-built bullet, `agents` module boundary, and testing coverage), not a full `/metis-reconcile`. Design calls it now records: the concrete `AnalystOutput` schema (`{posture, summary, key_points[], risks[], opportunities[], confidence∈{low,medium,high}}`, docs pin none); `MainAgentConfig` **reused** for analyst configs (rename → `AgentModelConfig` if touched again); the synthesis block via a `format_analyst_reviews` helper appended in `generate()`; the model-adapter test pattern (unit + `#[ignore]`d live smoke). The 16-skill library is pinned **unbuilt** there.
- *(deferred)* The **16 analyst skills** progressive-disclosure library (`docs/analyst-skills.md`) — the named follow-on slice now that the analyst stage exists.
- *(carried, low)* `SECTOR_PE_MAX` (120) calibration via live `tuning_sector_pe_distribution_probe`; `cargo fmt` dirty repo-wide + esbuild/vite advisory.

## Where to start

`main` is clean and pushed; **BUILD.md is current** (analyst layer folded in this session). Nothing owed — open a fresh direction. The natural next frontier is the **16-skill analyst progressive-disclosure library** (the deferred follow-on, now that the analyst stage exists); the parked `SECTOR_PE_MAX` calibration (live `tuning_sector_pe_distribution_probe`) remains the low-priority leftover.
