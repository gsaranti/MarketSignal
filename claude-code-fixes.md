# Notes for the next Codex docs review

Written 2026-07-11 after resolving all 21 findings of run 2; extended the same day after resolving all 10 findings of run 3, again after resolving all 5 findings of run 4, again after resolving all 7 findings of run 5, again after resolving all 5 findings of run 6, and again 2026-07-12 after resolving all 9 findings of run 7.
Purpose: what changed and what is deliberate, so the next review doesn't re-flag handled or by-design material.
Every fix follows the corpus's single-home discipline — the "canonical home" named per item is where the contract now lives; other mentions are pointers.
**Finding IDs restart every round** — the disposition tables below are per-round, so run 2's "P1-1" and run 3's "P1-1" are unrelated findings; match by content, never by ID.

## Disposition of the 9 round-7 findings

| # | Finding | Disposition | Canonical home now |
|---|---|---|---|
| P1-1 | The shared priced-stock scenario-target calculation has two incompatible specifications and no complete implementation contract | Fixed (function defined) | `portfolio-analysis.md` §Starting parameters — the **v2 rate-anchored scenario-target function** is now fully drafted as the single canonical contract both jobs compute: per-share **driver ladder** (consensus fwd EPS → FCF/sh → rev/sh; base = consensus mid, bear/bull = consensus low/high w/ dispersion fallback; v1 growth clamp as sanity bound) × **`DGS10` spread-anchored scenario multiples** (`M_s = 1/(spread_s + DGS10_now)` from the trailing ~3-yr / 12-quarter P25/P50/P75 of driver-yield − DGS10; ε = 1% degenerate guard and a ≥ 8-quarterly-observations floor, both falling back to raw percentiles, recorded; monotonicity-sorted), `TR_s` adding forward 12-month dividends — the dead-money decomposition computed as one function; one-month leg keeps v1 mechanics; versioned through the calibration snapshot's parameter version. The as-built v1 drift stays documented as-built (engine.rs comment names the docs-lead-code gap). **TO archetype driver overrides** drafted in `trade-opportunities.md` §Starting parameters (compounders → fwd EPS/P-E; ai-infra & disruptor → fwd rev/sh/P-S; commodity-cyclical → mid-cycle EPS ~5yr/P-E; the P/B / P/NAV read stays a scoring lens); implied-expectations re-specified as the v2 inversion; every DGS10 purpose clause (both workflows' Steps 2/5/5c/6b, both `data-sources.md` FRED tables) re-pointed in lockstep. Fund-form methodology stays the named open item — no leg of v2 has a fund input |
| P1-2 | Portfolio consumes and persists a risk tier without ever defining how a priced stock or equity fund receives Low/Medium/High | Fixed | `portfolio-analysis.md` §Starting parameters — **Risk-tier assignment**: a priced stock adopts TO's High/Low/else-Medium rule by pointer (`trade-opportunities.md` §Starting parameters stays the canonical constants, now carrying the adoption scope note) under a stated **missing-input rule** — an unavailable leg (event exposure; float/liquidity is enriching on Portfolio's surface) cannot trigger; wholesale-missing inputs → **Medium + logged tier-input gap** (neutral-imputation stance); a priced equity fund takes its own drafted mapping (High on leveraged/inverse flag, vol > 40%, drawdown > 50%, or illiquidity; Low on vol < 25% + liquid + no flags; else Medium); `role_risk_only` carries no tier. `portfolio-workflow.md` §Step 6b assigns and persists before the hurdle and Step 7a consume it |
| P1-3 | Model-authored ledger conditions have no executable schema or allowed-series registry despite the quick check's evaluate-everything promise | Fixed (narrowed) | The "no machine condition object / fields never specified" half was **refuted** — Portfolio already imports TO's condition-identity contract (metric + comparator + threshold + margin + count, `condition_id`) at §The position thesis ledger. The real gap: `portfolio-workflow.md` §Step 6g now **executability-validates every newly written or rewritten quantitative condition** (must resolve to a series the engine computes and refreshes, all fields well-formed; unresolvable → **downgraded to qualitative, logged, never dropped**, no machine evaluation state); §Step 6f Returns requires machine-evaluable statement; the quick check's "every quantitative condition" promise is anchored on only-validated-conditions-carry-the-type. The resolution basis is the shared round-7 resolution contract (P1-5) |
| P2-1 | A live FRED failure has no full-job outcome though the rate anchors are load-bearing and the failure matrix claims completeness | Fixed | `portfolio-analysis.md` §Failure posture — the **canonical rate-anchor rule** joins the hard class: a full/deep run hard-fails **before any per-item work** when `DGS10`/`DGS2` is still unavailable after the shared bounded retries; the engine-only quick paths (Portfolio quick check, ATO Quick Audit — both now explicit) fail-soft to the last cached print **only within the drafted ~1-week rate-cache max age** (§Starting parameters; house-view bound reused), typed `unknown` beyond. `trade-opportunities.md` §Failure posture carries the parallel class by pointer; both load sites (`portfolio-workflow.md` §Step 5, `trade-opportunities-workflow.md` §Step 2) and both `data-sources.md` FRED tables point |
| P1-4 | The cross-run research cache can satisfy a "fresh" pass with no reuse, invalidation, or timestamp rule | Fixed | `trade-opportunities.md` §Failure posture — the surviving cache is **document-level, deliberately narrower than Portfolio's distilled-findings reuse**: fetched/extracted pages keyed by normalized URL, the original retrieval timestamp the immutable vintage; every deep pass and Step-3b lane executes **current searches** (the cache satisfies only re-fetches); floor-bearing freshness must be met from currently searched results before any `last_deep_researched_at` stamp, forward-assumption restart, warning clear, or archival; the reused-vs-fresh split logged per pass. §Starting parameters row for discoverability; workflow §Step 5 intro / §Step 3c / ATO Deep Audit aligned; `storage.md` gains the **web-research document cache** store home (the adjacent both-jobs portability gap validation surfaced) and the TO audit's reuse-split field |
| P1-5 | Re-check classes are model assertions; neither leading-metric classes nor `filing` falsifiers are validated before maintenance treats them as refreshable | Fixed | `trade-opportunities-workflow.md` §Step 3c — the **resolution contract**, owned by the vocabulary's home: a class tag is an **app-validated claim** resolving against the engine's actually-refreshable surface (the closed `data-sources.md` endpoint-surface rows — no second enumerated table to drift), checked at **watchlist admission for hypothesis-card metrics** and at **§Step 5h for the opportunity's leading metric and `structured` AND `filing` falsifiers alike**; unresolvable → `research` + logged, never dropped; only resolution-backed conditions carry machine evaluation state or enter the swept-name population. The doc's own bogus `filing` example fixed (backlog → `research`-class; standardized statement lines are the class's examples). Portfolio's 6g validation rides the same contract by pointer |
| P1-6 | The evidence floor gates on "current" evidence with no executable thresholds, allegedly conflicting with recency-is-soft | Fixed (narrowed) | `trade-opportunities.md` §Starting parameters — the **evidence-floor freshness basis** row: quote/bars current through the latest completed session; structured/filing metrics + statements at the latest expected reporting period + a drafted **~45-day filing grace**; research leading metrics + bear-case evidence within the shared **~4-week** window; §Step 5h computes **`fresh` / `stale` / `freshness-unscorable`** from timestamps + cadence with named gap reasons; §Evidence floor points at the basis. The "conflicts with informs-never-gates" half was **overstated** — that rule is textually scoped to the tier gradient, so `web-research.md` gained a scoping sentence (claim freshness = a job-owned floor question, the deny-style carve-out; `freshnessSlaDays` stays weighting-only) rather than an exception carve-back. Portfolio's floor points at the same basis for its "stale prices" words |
| P2-2 | The price-tracked archive is absent from the endpoint surface's maintenance populations despite an every-run refresh promise | Fixed | `data-sources.md` §Trade Opportunities — endpoint surface — a **third maintenance population, the archive-price refresh**: one Stooq request per distinct archived symbol (≤ 100) after dedup against the swept and label-time populations, through the shared price-bar cache — the **job-time owner** of the archive's "each run" promise (the corpus's lean: five job-time clauses incl. round-6's per-run vs-sector refresh; render-time floor unchanged for page opens between runs); the Stooq equities row tagged in lockstep; `storage.md` §Stooq price-bar cache states the archive's job-time request rule beside the render-floor and label-time rules; `trade-opportunities.md` §Archived opportunities points at the population |
| P2-3 | The active-watchlist cap has no deterministic overflow victim or lifecycle transition | Fixed | `trade-opportunities-workflow.md` §Step 3c — cap enforcement **at add time, after normal pruning**: the **lowest hypothesis score retires first** (tie-break oldest successful metric refresh, then ticker — both persisted node fields), reason **`capacity-evicted`**, graph history retained, exactly **one retirement-class shadow episode** under the normal retirement anchors — the reason tag letting the retirement read segment involuntary stops from judged ones (`trade-opportunities.md` §Outcome learning). Constants row in §Starting parameters; `configuration.md` mirrors beside the shadow-ledger cap's eviction clause; `storage.md` and §Discovery memory stay pointers |

## Disposition of the 5 round-6 findings

| # | Finding | Disposition | Canonical home now |
|---|---|---|---|
| P1-1 | Durable outcome episodes don't retain the sector benchmark identity their labels require | Fixed | `trade-opportunities.md` §Outcome learning — sector classification and benchmarks resolve from each record's **entry-stamped sector identity**: the sector label already in hand at creation (per-candidate `profile`, else the surfacing feed's sector field) + the resolved SPDR benchmark symbol per the §Stooq mapping; refreshed on a live matrix record per deep pass, **frozen at entry on an episode** (freeze-at-last-seen stated; no new endpoint); a record with no valid mapping carries the typed **`sector-unscorable`** state (sector legs excluded / counted / logged, market + absolute legs unaffected). The stamp rides every durable field list in `storage.md` §Local Analysis Suite Storage — Portfolio episodes (both branches), TO matrix record, archive snapshot, shadow episodes, picked entry snapshot; the render-time floor and archive vs-sector refresh read the stamp; both label-time endpoint contracts state **no classification call** |
| P1-2 | "Age-bounded" active episode stores can pend forever and grow unboundedly | Fixed (narrowed) | `storage.md` §Local Analysis Suite Storage — pending is bounded by the shared **price-coverage grace** (drafted **~3 months past the window end**; the constant homed in `portfolio-analysis.md` §Starting parameters, shared by both jobs): a leg still pending past it closes as the typed **`price-coverage-unscorable`** label (counted / logged, excluded like its terminal / metric counterparts), so episodes freeze into their matured archives; the same bound is the transient-vs-disappearance discriminator (a stopped previously-covered series → the terminal contract; a never-covered symbol → the price-coverage state). TO's flat "each maturing within 12 months" corrected — one open episode per **lifecycle** (a departed lifecycle's episode matures on the same clock), closing by ~12 months + grace; `configuration.md` and `portfolio-workflow.md` §Step 7a aligned. The finding's delisted-series example was **refuted** (the conservative `terminal-unscorable` rule already closes factless disappearances) — the real gap was the never-covered symbol plus the missing discriminator |
| P1-3 | Settings save gate contradicts the cloud-keyless local-suite path | Fixed (docs; code slice named) | `configuration.md` §API Tokens — the two-token save-disable rule is **scoped to the cloud configuration submission** (agent models + API tokens), never Settings as a whole; the shared provider credentials and local-suite fields are declared **independently savable with no cloud token present** (§External Data Provider Credentials + §Local Analysis Suite Configuration carry the pointers). The as-built form persists all five credentials through the one token-gated save (`Settings.vue` `canSave` / `settings.rs` bail-before-write — comments updated to name the split), so the **provider-credential save split is a named code prerequisite of the queued Local-analysis-models Settings slice** — docs-lead-code, the holdings-netting pattern; the shipped Schwab section's independent credential save is the in-code precedent |
| P2-1 | A live metric-unscorable window can fall into `no-dominant-mode` despite the full-input invariant | Fixed | `trade-opportunities.md` §Outcome learning — branch 7 is **guarded on input completeness**: the residual claims only a window whose still-relevant branches were all evaluable; any still-relevant branch blocked by an unavailable input (a live `leading-metric-unscorable` window blocks branches 3/6, a `sector-unscorable` record branch 5) records the typed **`resolution-unscorable`** mode instead. First-match precedence is unchanged for evaluable branches — a forensic trip needs no metric, so partial-input windows stay claimable by branches 1–6; completeness binds only the residual. `resolution-unscorable` generalized: the typed mode for **any** window whose applicable branches couldn't all be evaluated — the live metric-unscorable window its live form, post-departure the limiting form. §Starting parameters row, `storage.md` clause (iii), and workflow §Step 7 aligned in lockstep. Not a round-5 re-flag: that fix was departure-scoped from birth; this closes the live path the recorded invariant already promised |
| P2-2 | ATO archival omits the picked-node `departed` transition | Fixed | `trade-opportunities.md` §Discovery memory — `departed` is **pass-agnostic**: taken in the same reconciliation pass as **any** deep-invalidation archival — DTO's Step 7 or an ATO Deep Audit's reduced continuity pass (matching storage.md's already pass-agnostic phrasing). `trade-opportunities-workflow.md` §ATO stage 4 — an `invalidated` Deep-Audit archival **atomically takes the touched picked node to `departed` under the same lifecycle id**, and the graph negation is scoped: **no discovery-wide rebuild** (no watchlist adds, no re-check retirements), the touched nodes' lifecycle transition its only mutation |

## Disposition of the 7 round-5 findings

| # | Finding | Disposition | Canonical home now |
|---|---|---|---|
| P1-1 | Normalized cost basis changes type across the Portfolio contract | Fixed | `schwab-integration.md` §What is pulled — cost basis is the **app-derived signed account-currency total** (`averagePrice × signed quantity`; Schwab reports the per-unit price), and book netting **sums signed totals across rows**, never a share-weighted average (dollar gain stays additive and well-defined at zero net quantity). `portfolio-analysis.md` §Storage and display extends the undefined rule to **non-positive netted basis** — %-gain and cash-invested go undefined, dollar gain stays (matches the shipped `cost_basis <= 0` frontend guard); manual-CSV bases pinned as totals; the "Schwab-reported" attributions corrected (market value reported, basis derived). Transcribes the code's existing convention (`schwab.rs` / `schwab_live.rs`) |
| P1-2 | Warning lifecycle makes blocking warnings both dismissible and condition-persistent | Fixed | `interface.md` §Persistent Warning Area — the lifecycle **splits by ownership**: condition-owned blocking categories (agent config, tokens, provider credentials, local-suite) are **gate state, no dismiss control, cleared only when the condition resolves**; the event-owned **failed-jobs category is the only dismissible one**, its dismiss targeting the rendered identity. Transcribes the as-built contract (`DISMISSIBLE_KINDS` / `config.rs is_blocking` / no-op blocking dismiss, all tested); the stale `types.ts` "two non-blocking categories" comment fixed to one |
| P2-1 | Debut-to-carry dedup validated against a carry's stale cell | Fixed | `trade-opportunities-workflow.md` §Step 6 + §Step 7 — an acceptance whose target is a **cheap-swept carry** is **provisional** (only the same-cell leg can go stale — the sweep never rewrites lineage / metric / catalyst); Step 7, after re-placement, **re-checks that leg against the carry's final cell**, a mismatch reinstating the debut in its own cell at its Step-6 rank (computed-only — the ranking pre-dates merges) and cancelling the **deferred** shadow-ledger write; union completeness counts reinstated debuts and requires **finally-confirmed** peers. The Step-6 collapse-target cards now carry their **assigned cell** and include the **inconclusive deep-read carries** (cells current from Step 5 — their collapses final at Step 6, like debut-into-debut); the collapse audit gains the **provisional → confirmed / reinstated** lifecycle with the second evaluation's inputs (`storage.md`) |
| P2-2 | Durable picked episode lacks the inputs `resolution_mode` needs | Fixed | `storage.md` §Local Analysis Suite Storage — the picked episode opens with an **entry calibration snapshot** (entry-vintage base-case target + methodology + tree parameter version, valuation / estimate-revision baseline, falsifier `condition_id`s with entry states, initial forensic state — Portfolio's calibration-feature-snapshot pattern ported) and **accumulates the dated branch-relevant events while the pick is live**; `trade-opportunities.md` §Outcome learning — the tree reads its non-price inputs **from the episode**, never a pruned run audit or a later run's re-derived values (hindsight); `thesis-played-out` pinned to the **entry-vintage** target; a post-departure window with no recorded terminal fact records the typed **`resolution-unscorable`** mode — excluded / counted / logged like its terminal and metric counterparts, never falling into `no-dominant-mode` |
| P2-3 | Maintenance I/O contract omits its network type and watchlist population | Fixed (undercount closed) | `trade-opportunities-workflow.md` — the §Step 7 **and §Step 3c** body type lines now read "Computed + API retrieval" (round-2's fix had reached the step table only; the finding caught Step 7, validation caught 3c too). `data-sources.md` §Trade Opportunities — endpoint surface — the sweep runs over **one union population, the swept names**: live matrix carries ∪ recheckable (`structured`/`filing`) watchlist nodes, `research`-class spending nothing, **dedup per distinct symbol after the union**, the watchlist independently capped and possibly larger than the matrix; rows re-tagged in the one vocabulary (`analyst-estimates` / `earnings` / Stooq equities per swept name; `quote` carried-matrix half only — watchlist relative strength reads Stooq bars; the filing-cadence rider fires for either half) |
| P2-4 | Precious metals had no owned retrieval path in Trade Opportunities | Fixed | **Stooq canonical**: gold `gc.f` / silver `si.f` join copper `hg.f` in the `data-sources.md` §Stooq identity table (same M5 live-verify convention); the TO futures row, Step 2's type line (which also gains the Stooq its own body already named) and body, and the keyless-feeds bullet updated in lockstep; the "Gold / silver remain FMP" sentence replaced with the **deliberate per-job split** — the report / Portfolio keep their FMP `GCUSD`/`SIUSD` point-in-time quote rows, while TO's cyclical sleeve needs a daily *series* for the turn read (and a price context pairing its run-level gold CFTC positioning row) |
| P3-1 | Retail-sales map row doesn't carry the shipped FRED identity | Fixed | `data-sources.md` §Planned report enrichment — the map is keyed on the **numeric FRED `release_id`** (new leading column on all 17 rows: 50 / 10 / 46 / 54 / 9 / 192 / 53, verified against the shipped `RELEASES` const), the display name presentation-only; retail sales corrected to the shipped **"Advance Monthly Sales for Retail and Food Services"** (ID 9, the sole mismatch of the seven); the join contract stated in-doc (numeric ID + verified FMP event string; the ID is in scope in the FRED fetch loop, or `EconomicRelease` gains an ID field — settled at wiring). The `RSAFS` **series** row deliberately untouched — it names a series, not a release. FMP event strings stay pending paid-key verification, unchanged |

## Disposition of the 5 round-4 findings

| # | Finding | Disposition | Canonical home now |
|---|---|---|---|
| P1-1 | `conflict handling` model-authored, undefined | Fixed | `portfolio-workflow.md` §Step 6d — the field is the typed two-value **`conflict_handling`** declaration (`supplement` / `supersede`), a claim the engine validates, never a rule the model selects; §Step 6e holds the **app-owned conflict policy**: `supplement` never displaces a present feed value; `supersede` honored only when the engine verifies a strictly-newer as-of, a primary-source fact-type whitelist (issued guidance / signed contract / filed figure — drafted), and metric/units/period identity; **structured-wins is the default**, every rejection logged with its failed condition, the matched rule recorded in the audit record (`storage.md`). TO §5f points at §6e |
| P2-1 | Step-6 dedup collapse semantically unvalidated | Fixed | `trade-opportunities-workflow.md` §Step 6 — **collapse eligibility is app-validated on a typed equivalence predicate**: same assigned cell AND a shared hypothesis-lineage node / identical leading-metric series identity / same typed catalyst; the free-text reason is recorded color, never the acceptance basis; an invalid proposal **defaults to list-both** (fail-open to redundancy, never omission); predicate inputs + validator result persist in the collapse audit (`storage.md`); the settled direction rule stays a separate invariant. TO §The pipeline mirrors by pointer |
| P2-2 | Shadow label-time Stooq missing from endpoint surface; "no per-symbol spend" contradicts the cache contract | Fixed (narrowed) | `data-sources.md` §Trade Opportunities — endpoint surface — the label-time outcome refresh covers **picked and shadow episodes alike** (all five turn-away classes), **per distinct symbol after dedup** through the shared cache, bounded to windows maturing since the last run; the Stooq equities row tagged in lockstep; both "no per-symbol spend" clauses (TO §Outcome learning, workflow §Step 7) narrowed to **no FMP / research / model spend** with the label-time Stooq request named as the one touch; `storage.md`'s canonical rule now says "picked or shadow alike". The finding's operational claim (shadow stale by design) was **refuted** — storage.md's rule already covered shadow on a plain reading; the fix aligns the narrower surfaces to it |
| P2-3 | `resolution_mode` tree asserted but never specified | Fixed | `trade-opportunities.md` §Outcome learning — the **ordered, first-match-wins tree, evaluated independently per matured window**: terminal typing → `forensic-materialization` → `leading-metric-rollover` (a lucky win lands here deliberately) → `multiple-unwind` → `market-beta` → `thesis-played-out` → the explicit **`no-dominant-mode`** residual (total function; residual share is itself a calibration read). Branch constants drafted in §Starting parameters (loser bar −10% — market-relative for unwind, absolute for beta; unwind = ≥60% multiple-contraction share w/ revisions in a ±5% band; beta = within ±5pp of sector; played-out = Stooq close at/above the stored base-case target w/ metric persisted); precedence itself calibratable. Workflow §Step 7 points at it |
| P3-1 | Release→event map declared codeable with no rows | Fixed | `data-sources.md` §Planned report enrichment — the complete **17-row drafted table** over the 7 tracked releases (surfaced in-doc from the shipped `RELEASES` const), columns = FMP event string / unit normalization / polarity / epsilon, under the **Stooq-identities convention** (drafted in-doc, live-verified before wiring): FMP event strings + raw scales **pending paid-key verification, a named blocking prerequisite**; the polarity convention is stated (activity `higher-favorable`, inflation `lower-favorable`, regime-inverting prints `none`); a wrong string fails soft to names + dates, never a wrong classification |

## Disposition of the 10 round-3 findings

| # | Finding | Disposition | Canonical home now |
|---|---|---|---|
| P1-1 | Holdings consolidation undefined | Fixed (docs; code slice named) | `schwab-integration.md` §What is pulled — **book-level netting per symbol at snapshot assembly**: signed quantities + market values sum across granted accounts and manual supplements, cost basis share-weighted, long/short classified from the netted quantity, Schwab-authoritative identity fields, manual rows merge tagged-by-source with the overlap surfaced to the user; per-source rows retained for display/audit only, every downstream contract consumes the normalized book rows. The as-built adapter concatenates without netting (`schwab_live.rs` + `diff.rs` last-row-wins on duplicate symbols — a latent multi-account code bug), so the netting step is a **named prerequisite of the full Portfolio slice**, deliberately not yet implemented |
| P1-2 | Shared sourcing still demoted the CIK resolver | Fixed | `data-sources.md` §Local analysis suite — shared sourcing now states the job split inline (Portfolio prerequisite / TO non-blocking), pointing at §SEC EDGAR — the one pre-P1-6 (run-2) sentence that fix missed |
| P1-3 | Post-maturity episode has no legal state | Fixed (narrowed) | `portfolio-analysis.md` §Outcome learning — the **no-active-episode state is codified**: an episode is a bounded 12-month measurement instrument; extension applies only while one is active; a post-maturity re-affirmation records nothing; the next state change opens a fresh episode (TO's one-open-episode-per-live-lifecycle pattern, now cross-referenced); a post-maturity falsifier confirmation records on the **thesis ledger**, amending the "lives with the episode" rationale |
| P2-1 | Maintenance endpoint surface can't serve the filing-cadence promises | Fixed | `data-sources.md` §Trade Opportunities — endpoint surface — the sweep gains a **conditional filing-cadence rider**: the swept `earnings` row doubles as the fresh-filing trigger; a name whose latest report postdates the last sweep refreshes its statements / `key-metrics` / `ratios` / `financial-scores` / `financial-growth` rows (tagged inline); SEC submissions / company-facts stay per-candidate (TO's CIK resolver stays non-blocking). Step 7's "tier … inputs refresh every run" overstatement corrected to price-side-every-run / statement-side-on-the-rider |
| P2-2 | DTO resume pins nothing | Fixed | `trade-opportunities.md` §Failure posture — resume is its own entry path reopening the interrupted run's ID, pinning the Step-2 shared context, Step-3 feeder outputs + route plan, Step-4 slate / budget / rotation backlog, source as-of stamps, and model/prompt/schema versions; offered within the **~48h window shared with Portfolio** (single constant, Portfolio §Starting parameters); a new Discover discards checkpoints (research cache survives); ATO Deep Audit resumes under the same contract; Quick Audit never checkpoints |
| P2-3 | Archived pick's graph node had no transition | Fixed | `trade-opportunities.md` §Discovery memory — fourth terminal node status **`departed`**, set in the same Step-7 reconciliation pass that archives; visible in route context as a dead thesis (anti-blind-rederivation), never a discovery feeder, never re-promotable in place, **excluded from shadow-ledger scoring** (the picked episode owns the outcome); fresh re-entry = new node + new lifecycle; tombstones pruned on the archive's retention (the uncited picked-node boundedness gap, also closed) |
| P2-4 | Quick Audit "reads no Schwab data" vs Step-8 pull | **Refuted** (wording tightened) | All three sentences were already scoped to the cheap re-derivation, and the workflow's intro states the ATO holdings read outright. The one misreadable clause (workflow §ATO "needing only FMP / Stooq / FRED at runtime") now says "for the analytical pass" and names the Step-8 fail-soft, display-only pull as the run's one Schwab touch |
| P2-5 | Winner value inside the loser-scoped enum | Fixed | `trade-opportunities.md` §Outcome learning — the field is the all-outcome **`resolution_mode`**, assigned to every matured pick by the deterministic tree; `thesis-played-out` is the winner branch; "for the losers" dropped; workflow §Step 7 / DTO overview and `storage.md` renamed in lockstep (the generic phrase "the failure mode of stateless re-discovery" at TO §Discovery memory is ordinary English, not the enum) |
| P3-1 | One-to-many calendar fan-out vs scalar surprise fields | Fixed | `data-sources.md` §Planned report enrichment — the map row is one **release ↔ event pair** (per-event unit normalization / polarity / epsilon); `EconomicRelease` gains **`surprises[]`** keyed by the map's event name (estimate / actual / relation / **absolute gap** / % gap / beat-miss), empty default under `#[serde(default)]`, deduplicated per event name (latest print wins); the absolute gap now appears in the persist enumeration; Step-3, Step-16, and router prose updated in lockstep |
| P3-2 | Prompt prose misstates the performance half | Fixed | `report-workflow.md` §Step 16 — "the P/E against its own trailing-~1yr range **paired with the cumulative return over that trailing window**" (no range read on the return); the router note and the Step-8 contract prose (an uncited third touchpoint) both carry the paired semantics and the re-rating-turn vs value-trap routing distinction |

## Disposition of the 21 round-2 findings

| # | Finding | Disposition | Canonical home now |
|---|---|---|---|
| P1-1 | Shorts long-only downstream | Fixed (scope-out) | `portfolio-analysis.md` §Asset eligibility — net-short equities are **not rated** (short reason); signed exposure feeds the roll-up; a long↔short reversal force-includes in selective runs; outcome tag vocabulary gains `reversed` |
| P1-2 | Failed sweep = silent clear | Fixed | `portfolio-analysis.md` §The quick check — typed per-family sweep states `fresh_clear` / `flagged` / `unknown`; `unknown` force-includes like a flag; degraded-sweep note in the Research-stale badge family |
| P1-3 | FI/option risk unsourced | Fixed (narrowed) | `portfolio-analysis.md` §Portfolio roll-up and construction — contribution = market value + signed notional (OCC-derivable); duration / credit / **standalone**-option delta are **typed gaps by design** (no on-plan source; held-underlier overlay delta from its own chain is the exception) |
| P1-4 | Vector memory isolation | Fixed | `storage.md` §Local Vector Memory — entry kind = purpose boundary (summary → dossier recall; learning → calibration only); TO rows lifecycle-tagged; **embedder identity, never dimension**, is the compatibility key (re-embed from retained content on change) |
| P1-5 | Step 7b model-authored sizing | **Refuted** | Invariant is scoped to engine-owned *analytical* values (`local-models.md` §Context-memory discipline); bounded sizing is a documented model decision under the joint-feasibility check. Clarifying clause added at `portfolio-workflow.md` §How to read |
| P1-6 | EDGAR CIK resolution | Fixed | `data-sources.md` §SEC EDGAR — the cached `company_tickers.json` resolver is a **named prerequisite of the full Portfolio slice**; unresolved/ambiguous CIK → typed unknown feeding the degraded-sweep rule; SEC endpoint rows tagged "CIK-gated" |
| P1-7 | Fund floor vs bond/commodity | **Refuted** | The carve-out is stated at §Evidence floor (the sentence after the fund analog); routing precedes the floor; the floor input is sector/country weightings (not holdings weights). Scope words added ("exposure-priced (equity-fund) branch") |
| P1-8 | Stooq underspecified | Fixed (narrowed) | `data-sources.md` §Stooq — `^spx`, the SPDR sector-ETF mapping from FMP sector labels, copper `hg.f`, split-adjusted / dividend-unadjusted, all single-homed there and flagged for M5 live verification. Adjustment semantics + fallback were already specified (§Outcome learning / §Stooq) — only symbols and the mapping were missing |
| P1-9 | Durable vectors vs run retention | Fixed | `storage.md` §Local Vector Memory "Own retention" — kind-based carve-out: durable-learning rows tie to their episode stores, not the run window (both local namespaces) |
| P2-1 | Segment-series ingestion | Fixed | `trade-opportunities-workflow.md` §Step 5d/5e/5f — typed `leading_metric_observation` returned at 5e, app-validated + appended, **Step 5f recomputes** the leading-metric read and `business_runway` (Portfolio-6e-style carve-out) before the 5h gate/floor; segment series' recheck class is **`research`** (`filing` = engine-refreshable model-free only); stored series has a schema home in `storage.md` |
| P2-2 | "Computed" mislabeled network-free | Fixed (narrowed) | `trade-opportunities-workflow.md` §How to read — "engine-only" = *no generative model, never no network*; Steps 3c/7 tagged `Computed + API retrieval`; maintenance/outcome cardinalities added to the TO endpoint surface; label-time coverage rule now held by **both** jobs (`storage.md`, TO §Outcome learning). ATO Quick Audit was already honest (names FMP/Stooq/FRED) — that citation was wrong |
| P2-3 | Dedup before union | Fixed | §Step 6 — cheap-swept carries join the dedup input as **collapse targets only**; direction app-enforced (debut may collapse into a carry's lifecycle; a live carry never collapses away); direction recorded in the collapse audit; Step 7 stays computed-only |
| P2-4 | Picked episodes outlive metric-label path | Fixed | `trade-opportunities.md` §Outcome learning — metric label populated per recheck class + lifecycle state; **`leading-metric-unscorable`** (counterpart of `terminal-unscorable`) for windows with no legal refresh path, excluded from calibration denominators; the archive stays price-only |
| P2-5 | Rotation max-age guarantee | Fixed (wording) | §Step 4 — max-age = **best-effort service target under the run's budget**; overflow forms a stalest-first backlog, surfaced (count + oldest age); liveness ("never ages indefinitely") is the only guarantee kept; all hard-bound phrasings removed |
| P2-6 | Picked episodes portability + cap | Fixed (narrowed) | Portability was already covered by the format-extension rule — the stale enumeration at `data-portability.md` §Build-order placement now lists the picked stores; the dangling "its own cap" resolves to the new **picked matured-archive retention (drafted 5,000)** row in TO §Starting parameters + a configuration.md knob |
| P2-7 | Event route selected before evidence | Fixed (narrowed) | Speculative route choice + in-route dormancy is the design; the defective clause (`:164`-area false parallelism) now says the app enforces the materiality gate **at card formation** against typed material-event evidence carried on the card |
| P2-8 | Horizon "model-authored" | Fixed | §Step 7 — `expected_thesis_realization` is rule-derived at the last deep pass's 5h from deep-pass inputs the cheap sweep never refreshes (freeze behavior unchanged; the tier's contrast stated) |
| P2-9 | FMP Articles "keyless" | Fixed | `data-sources.md` §FMP Articles + the TO endpoint row — free-tier but **authenticated on the shared FMP key**, "no new credential", never "keyless" (adapter code confirms: `fmp_news.rs` sends `apikey`) |
| P3-1 | `averageChange` unit bug | Fixed | §Planned report enrichment — `Π(1 + averageChange / 100) − 1`, serialized as a decimal fraction; ±25-percentage-point inadmissibility guard added to the single-homed rule set |
| P3-2 | Consensus double fallback | **Refuted** (wording tightened) | The `§endpoint surface` sentence referred to the narrative layer; it now says so explicitly — structured calendar fields stay names + dates on a missing estimate, never model-filled |
| P3-3 | Step 3 group miscount | Fixed | `report-workflow.md` §Step 3 — three enrichment bundles across **five** groups; the sector trailing-return is re-homed to **sector performance** (`SectorPerformance`), industries carry both reads on one snapshot |

## Deliberate designs — do not re-flag

- **Net-short equities are not rated** — a scope decision, not an omission. Signed exposure still reaches the roll-up; reversals force-include; the `reversed` alignment tag is excluded from aligned/contrary cohorts.
- **Duration / credit / standalone-option delta are typed gaps** — mirrors the bond-fund valuation gap; adding a bond-analytics source was considered and rejected (no verified on-plan source).
- **The event-impact route is chosen speculatively** — planning is deliberately seed-free and spends no fetch budget; the materiality gate is enforceable only at card formation, and a gate-less route staying dormant is by design.
- **Step 7 / ATO Quick Audit make network calls while "engine-only"** — "engine-only" is now defined as *no generative model*; the `+ API retrieval` type tag is the network marker.
- **The rotation max-age is best-effort, not a wall-clock bound** — DTO is user-run and the budget fixed; liveness + surfaced backlog is the whole promise.
- **The archive is price-only after departure** — a still-maturing pick's metric label deliberately freezes and records `leading-metric-unscorable`; post-departure metric re-pulls were considered and rejected to preserve the archive invariant.
- **Segment-revenue quarterly observations are research-extracted** — a deterministic 10-Q segment extractor was considered and rejected (non-uniform disclosures); `filing`-class membership requires model-free refreshability, so segment anchors are `research`-class.
- **A live carry never collapses away in dedup** — the direction rule is an invariant (matrix exit = deep invalidation only), not an oversight.
- **Bounded sizing is a model decision** — the compute-every-number invariant covers engine-owned analytical values only; Step 7b's target weight is engine-bounded, model-chosen, joint-feasibility-validated.
- **The SEC cross-check for Trade Opportunities remains non-blocking** — only Portfolio promotes the CIK resolver to a prerequisite.
- **Holdings identity is the book-level netted row** (round 3) — per-account and per-source truth is retained for display/audit only; a docs-vs-code gap against the as-built concatenating adapter is *known and named* (the netting step is a stated prerequisite of the full Portfolio slice), so flagging that mismatch adds nothing.
- **`departed` graph nodes stay visible in route context** (round 3) — deliberate anti-blind-rederivation, not an anti-reflexivity leak: the node is never a feeder, never re-promotable in place, and excluded from shadow scoring; the fresh-start rule governs *influence on a new lifecycle*, not knowledge that a thesis died.
- **`resolution_mode` is all-outcome by design** (round 3) — one field, one exhaustive deterministic tree over every matured pick; `thesis-played-out` is its winner branch, not a scope error.
- **A matured, unchanged Portfolio holding carries no active episode** (round 3) — deliberate; re-affirmations over a matured decision record nothing, mirroring TO's immutable-episode bound.
- **The resume window is one shared ~48h constant** (round 3) — homed in Portfolio §Starting parameters; TO points at it rather than drafting its own.
- **The filing-cadence rider triggers off the swept `earnings` row** (round 3) — deliberately not SEC submissions, so TO's CIK resolver stays non-blocking; FMP's symbol-keyed statements are the model-free refresh path.
- **The conflict policy is app-owned; `conflict_handling` is a validated claim, not a rule selection** (round 4) — the model declares `supplement` / `supersede`, the engine decides; flagging that the model "authors" the field misses that no declaration can bypass the engine-verified conditions.
- **Collapse equivalence requires typed identity** (round 4) — "near-identical thesis" prose alone can never remove a gate-clearing name; a proposal failing the predicate lists both, by design (redundancy over omission).
- **`no-dominant-mode` may absorb many ordinary windows** (round 4) — deliberate: calibration reads the tails, and the residual share is itself a surfaced read, not a silent bucket; the tree's precedence order is a drafted, calibratable constant, and its attribution bias (forensic-first undercounting unwind) is known and stated.
- **A lucky win resolves as `leading-metric-rollover`** (round 4) — a price winner whose anchor reversed is a gate miss, not a validation; the mode describes how it resolved, not whether it made money.
- **The event-map polarity convention is regime-aware** (round 4) — wages and JOLTS openings are `none` deliberately (growth-positive but rate-hawkish prints have no regime-free favorable side); flagging missing polarities on those rows would misread the convention.
- **Dollar gain stays defined on a non-positive netted basis** (round 5) — signed cost-basis totals keep market value − cost basis equal to aggregate unrealized P/L even at zero net; only the ratio reads (%-gain, cash-invested) go undefined there, by design.
- **`quote` deliberately excludes the watchlist half of the sweep** (round 5) — watchlist relative strength reads Stooq daily bars; a quote row spends only on the carried-matrix half's gate re-runs. Not an omission.
- **The gold / silver sourcing split is per-job and deliberate** (round 5) — report / Portfolio keep FMP point-in-time quotes; Trade Opportunities reads daily Stooq series (`gc.f` / `si.f`) because its turn read needs a series. Flagging the divergence as an inconsistency misses the stated rationale.
- **Only cheap-swept collapse targets go provisional** (round 5) — a collapse into a debut or an inconclusive deep-read carry is final at Step 6 (both cells current from this run); the provisional lifecycle is scoped to the one population whose cell Step 7 can move.
- **`resolution-unscorable` vs `no-dominant-mode`** (round 5) — missing branch inputs are a typed, counted exclusion (the terminal-/metric-unscorable pattern), never the residual; `no-dominant-mode` remains reserved for windows with full inputs that no branch claims.
- **Shadow episodes carry no calibration snapshot** (round 5) — deliberate asymmetry: their labels are price-only, so they need no target / valuation / falsifier inputs; only the picked episode carries the snapshot the `resolution_mode` tree re-tests.
- **The sector stamp freezes at last-seen** (round 6) — an episode's benchmark never moves on a later sector reclassification (the entry-vintage philosophy, matching the entry-vintage target), and the stamp costs no new endpoint (it records classification the funnel already held — a deferral-class shadow episode stamps from the surfacing feed's sector field). Flagging "stale sector mappings" or a missing label-time `profile` call misreads the stated approximation.
- **Affirmative branches may claim partial-input windows** (round 6) — the input-completeness guard binds only the residual (branch 7); a forensic trip needs no metric, so branch 2 legitimately claims a metric-unscorable window. Flagging that as a guard violation misreads the rule.
- **The v2 target function is rate-anchored multiples, not a cash-flow DCF** (round 7) — deliberate: DGS10's long-duration sensitivity lives in the spread-anchored multiple, avoiding an ERP constant, fade rules, and terminal-growth assumptions a multi-year DCF would need. Flagging "the docs promise DCF math but define multiples" misreads the settled shape; the old "scenario / DCF" phrasings were re-pointed with it.
- **The research-cache asymmetry is per-job and deliberate** (round 7) — TO's cross-run cache is document-level (deep passes carry archival authority, so distilled judgment is never carried across runs), while Portfolio reuses distilled findings per holding under its evidence-event invalidators (its re-analysis has no archive to poison). Flagging the two contracts as an inconsistency misses the stated rationale.
- **Capacity eviction is a reason-tagged retirement, not a sixth decision class** (round 7) — the shadow taxonomy stays five classes; `capacity-evicted` rides the retirement class so the retirement read can segment involuntary stops without proliferating classes.
- **The archive-price refresh is job-time-owned** (round 7) — the render-time floor is a second, lazy reader for page opens between runs, never the refresh owner; the two rules coexist by design (the live matrix already holds the same dual pattern).

## New drafted constants / typed states (deliberate drafts, shadow-tunable)

- Quick-check sweep states `fresh_clear` / `flagged` / `unknown` (per required signal family).
- `observed_net_alignment` value `reversed`.
- `leading-metric-unscorable` (metric-label availability), plus the TO label-time price-coverage **pending** rule.
- `leading_metric_observation` typed object `{ metric, period, value, units, filing / as-of date, source URL, confidence }`.
- Picked matured-archive retention: **5,000** rows (mirrors the shadow matured archive).
- `averageChange` sanity guard: **±25 percentage points**.
- Stooq identities: `^spx`, SPDR sector ETFs (`xlk.us` … `xlc.us`), copper `hg.f` — **pending M5 live verification**, stated in-doc; flagging them as *unverified* is redundant, flagging a factual error in the mapping is welcome.
- (Round 3) Graph node status **`departed`**; the all-outcome **`resolution_mode`** field (renamed from the loser-scoped "failure mode"); **`EconomicRelease.surprises[]`** per-event entries (estimate / actual / relation / absolute gap / % gap / beat-miss); the **book-level netted holdings row** (+ per-source rows for display/audit); the TO **resume pin set** (Step-2 context, Step-3 outputs + route plan, Step-4 slate/budget, as-of stamps, versions) under the shared ~48h window; the maintenance **filing-cadence rider** row set (statements / `key-metrics` / `ratios` / `financial-scores` / `financial-growth`).
- (Round 4) The **`conflict_handling`** enum (`supplement` / `supersede`) + the supersede whitelist (issued guidance / signed contract / filed figure); the Step-6 **collapse equivalence predicate** (same cell + lineage node / metric identity / typed catalyst); the **resolution-mode tree constants** (loser bar **−10%**, unwind share **60%** w/ **±5%** revisions band, beta band **±5pp**, the target-hit predicate) + the residual **`no-dominant-mode`**; the **release→event map row set** (17 rows over 7 releases — FMP event strings and raw scales **pending paid-key live verification**, stated in-doc; flagging them as *unverified* is redundant, flagging a factual polarity/unit error is welcome).
- (Round 5) The typed **`resolution-unscorable`** mode; the picked episode's **entry calibration snapshot** field set (entry-vintage target + methodology + tree parameter version, valuation / revision baseline, falsifier `condition_id`s + entry states, initial forensic state) plus its **accumulated dated live events**; the collapse audit's **provisional → confirmed / reinstated** lifecycle; the sweep-population term **"swept names"** (live carries ∪ recheckable watchlist nodes, symbol-deduped after the union); Stooq **gold `gc.f` / silver `si.f`** (joining the M5 live-verify set); the release→event map's **`release_id` column** (50 / 10 / 46 / 54 / 9 / 192 / 53, from the shipped const); the signed-total **cost-basis derivation** (`averagePrice × signed quantity`, summed across rows) with the non-positive-basis undefined rule.
- (Round 6) The **entry-stamped sector identity** field pair (sector label + resolved SPDR benchmark symbol) on every durable episode / matrix / archive record, with the typed **`sector-unscorable`** state; the typed **`price-coverage-unscorable`** label + the shared **price-coverage grace** (drafted **~3 months** past the window end, homed in Portfolio §Starting parameters); the residual's **input-completeness guard** (`resolution-unscorable` generalized to any window with an unevaluable still-relevant branch).
- (Round 7) The **v2 rate-anchored scenario-target function** and its drafted constants (driver ladder fwd EPS → FCF/sh → rev/sh; ~3-yr / 12-quarter anchor window; P25/P50/P75 driver-yield spreads through run-level DGS10; ε = **1%** degenerate guard; **≥ 8** quarterly observations for the rate correction; monotonicity sort; forward-12-month-dividend total-return leg) + the **TO archetype driver overrides** (mid-cycle window drafted **~5yr**); the **Portfolio risk-tier assignment** (TO-rule adoption w/ the missing-input rule; the fund mapping's vol/drawdown/liquidity/structural-flag legs); the **rate-cache max age** (~**1 week**, engine-only quick paths); the **evidence-floor freshness basis** (session-current quotes; latest expected reporting period + ~**45-day** filing grace; the ~4-week research window) + the typed **`freshness-unscorable`** state; the **watchlist cap eviction** rule + the persisted **`capacity-evicted`** retirement reason; the **archive-price maintenance population** (endpoint surface, third population); the **document-level research cache** contract (normalized-URL key, immutable vintage, current-searches-always, reuse-split audit leg) + its `storage.md` store home.

## Known open items (named in-doc; re-flagging adds nothing)

- **Fund-form scenario-target methodology** — the fund slice's named blocking input (§Asset eligibility), deliberately open.
- **Scorecard display surfaces** (Portfolio + TO) — deferred, stated in both §Outcome learning sections.
- **Stage-and-swap import hardening** — named, unscheduled (data-portability).
- **Factor-exposure model + portfolio stress reads** — deferred together (§Starting parameters).
- **M5-gated live verifications** — local-suite smoke, Stooq symbol map, serving pre-flight.
- **Release→event map string verification** (round 4) — the calendar enrichment's named blocking prerequisite, folded into the paid-key checkpoint; the drafted rows deliberately lead the verification.
- **Holdings-normalization code change** (round 3) — the book-level netting step in the live adapter (plus a duplicate-symbol guard in the diff) is a named prerequisite of the full Portfolio slice; the docs contract deliberately leads the code.
- **Settings provider-credential save split** (round 6) — the ungated FMP / FRED / Tavily save is a named code prerequisite of the queued Local-analysis-models Settings slice; the shipped token-gated form is a known, stated docs-vs-code gap, so flagging that mismatch adds nothing.

## Notes on review mechanics

- Anchor sweep after the fixes: 1,119 relative links, 0 broken (GitHub slugging, duplicate-heading suffixes included).
- Two of round 2's citations were mis-located and worth avoiding as anchors next time: portfolio-workflow §Step 2 contains no EDGAR reference (the quick check's EDGAR sweep lives in `portfolio-analysis.md` §The quick check), and trade-opportunities §Continuity and isolation contains neither the uncapped-matrix nor the user-run fact (those live in §The opportunity space / §The two jobs).
